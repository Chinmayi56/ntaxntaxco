# Local development (works on localhost and LAN/network URLs):
REACT_APP_API_URL=http://localhost:8001/api

# Production when frontend and backend share the same domain/proxy:
# REACT_APP_API_URL=/api

# Production when backend is deployed separately:
# REACT_APP_API_URL=https://YOUR_NTAXCO_BACKEND_DOMAIN/api
